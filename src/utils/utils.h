#ifndef UTILS_H
#define UTILS_H
#include "../sysinfo/user.h"
#include <sol/sol.hpp>
#include <string>

// std::string replace(const std::string &str, const std::string &search,
//                     const std::string &replace);
// std::string replaceVars(std::string str);
// std::string parseArgs(int argc, char *argv[], std::string &configFile,
//                       sol::optional<std::string> &output);

// extern const std::string version;
// extern const std::string default_config;
// extern const std::string help_message;

#endif
